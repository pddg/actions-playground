name: Test Native Egress
on:
  push:

jobs:
  run:
    runs-on: ubuntu-24.04-firewall
    permissions: {}
    steps:
      - run: |
          dig github.com
          dig google.com
      
      - run: |
          curl -v https://github.com
          curl -v https://google.com
        env:
          NO_PROXY: "*"
